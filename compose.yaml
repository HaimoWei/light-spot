services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
      PORT: "3000"
    env_file:
      - .env.local
    ports:
      - "3000:3000"
    restart: unless-stopped
