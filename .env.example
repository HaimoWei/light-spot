# SMTP (Contact form)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=weihaimoau@gmail.com
SMTP_PASS=YOUR_APP_PASSWORD
SMTP_SECURE=true

# Where the contact form emails are delivered to
CONTACT_TO=weihaimoau@gmail.com

# Sender address shown in the email (often must match/align with SMTP_USER)
CONTACT_FROM="Light-Spot <weihaimoau@gmail.com>"

# Optional: anti-abuse rate limiting (best-effort in-memory)
CONTACT_RATE_LIMIT_WINDOW_MS=600000
CONTACT_RATE_LIMIT_MAX=3
CONTACT_RATE_LIMIT_MIN_INTERVAL_MS=30000
